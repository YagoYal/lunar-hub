---
const year = new Date().getFullYear();
---

<footer id="about" class="site-footer">
	<div class="footer-inner">
		<div>
			<strong>Lunar Hub</strong>
			<p>
				Curadoria de ciência lunar de fontes públicas. Conteúdo de
				terceiros segue a licença de cada fonte original.
			</p>
		</div>
		<ul class="footer-links">
			<li><a href="#methodology">Metodologia</a></li>
			<li>
				<a href="https://lunar-ice.vercel.app">Lunar Ice Intelligence</a>
			</li>
		</ul>
	</div>
	<p class="fine-print">
		© {year} Lunar Hub — conteúdo próprio sob CC BY-NC 4.0 (uso não
		comercial). Pesquisas de terceiros pertencem às suas fontes originais.
	</p>
</footer>

<style>
	.site-footer {
		border-top: 1px solid var(--border);
		background: var(--surface-alt);
		padding: var(--space-5) var(--space-3) var(--space-4);
	}

	.footer-inner {
		max-width: var(--content-width);
		margin: 0 auto;
		display: flex;
		flex-wrap: wrap;
		justify-content: space-between;
		gap: var(--space-4);
	}

	.footer-inner p {
		max-width: 24rem;
		font-size: 0.875rem;
	}

	.footer-links {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: var(--space-1);
		margin: 0;
		padding: 0;
	}

	.footer-links a {
		text-decoration: none;
		font-size: 0.9375rem;
	}

	.fine-print {
		max-width: var(--content-width);
		margin: var(--space-4) auto 0;
		font-size: 0.8125rem;
		color: var(--text-muted);
	}
</style>
